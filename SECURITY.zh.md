# 安全策略

> 本文为 [SECURITY.md](SECURITY.md) 的中文译本。

本文帮助你理解 Pi 背后的安全模型，以及边界在哪里。

总体而言，Pi 是在启动它的用户安全边界内本地运行的 coding agent。由用户负责监控其操作，或把它放进容器、虚拟机或其他沙箱方案。

Pi 把本地用户账户、以及该账户可写的文件，视为与 Pi 进程处于同一信任边界。若攻击者能修改用户家目录、工作区、shell 启动文件、环境变量或 Pi 配置下的文件，通常就能影响 Pi 或其他本地开发工具。依赖这种“事先已有本地写权限”的报告不算安全漏洞，除非能证明 Pi 本身授予了该写权限，或跨越了操作系统特权边界。

Pi 依赖用户安装可信扩展、加载可信 skill，并且只在受信任的仓库中使用。因为 `AGENTS.md` 或注释里的指令可以轻易对 coding agent 做 prompt injection，这一点无法防护。

## 报告漏洞

若你认为在 pi 或本仓库其他包中发现了安全漏洞，请通过以下方式**私下**报告：

- 发邮件到 `security@earendil.com`，或
- 通过本仓库的 GitHub Security Advisories 提交私下报告

请包含：

- 问题描述及其影响
- 复现步骤、概念验证或相关日志
- 受影响的包、版本、commit 或配置
- 已知缓解措施

不要为安全敏感报告开公开 issue。我们会审阅报告并视情况协调披露。

## 范围内

已分发的软件包、命令行工具、API、仓库代码中的安全问题，以及 Earendil 在 `pi.dev` 上运营的基础设施，均在范围内。

## 范围外

- 本地代码执行或沙箱行为（Pi coding agent 有意不带沙箱）
- 用户自行安装的 pi 扩展或 skill 的行为
- 在不受信任仓库中工作的风险
- 安装不受信任的扩展、skill、包或工具的风险
- 不可信 MITM 代理导致的问题
- 将 Pi 安装暴露到公网
- Prompt injection 攻击
- 暴露的密钥若是第三方 / 用户控制的凭证
- 需要具备在目标机器上创建、修改、删除或替换文件、目录、符号链接、环境变量、shell 配置或其他用户可控本地状态的能力的报告。这包括 `~/.pi`、`~/.pi/agent/models.json`、工作区文件、`AGENTS.md`、skills、扩展、扩展配置、dotfiles，以及通过 NFS、漫游配置或 dotfile 管理器同步的文件，除非报告能证明是 Pi 本身授予了该访问。
- 用户故意弱化配置导致的问题。
- 需要针对 pi coding agent 使用受信任本地输入 / 配置才能成立的资源耗尽 / DOS 声称。
- 关于恶意模型输出的报告。
- 把用户批准或用户发起的本地操作说成漏洞。

## 给报告者的说明

最有用的报告能展示**当前可复现**的安全边界绕过，并证明影响。只展示本地 agent 的预期行为、prompt injection，或恶意但受信任的扩展 / skill 的报告，在本模型下不算安全漏洞。

例如：报告显示“向受信任的 Pi 配置文件写入恶意内容会导致 Pi 执行命令、加载攻击者控制的工具、把凭证发到攻击者控制的端点，或改变行为”——这属于范围外。

尽可能包含确切受影响路径、包版本或 commit SHA、配置，以及针对最新 release 或最新 `main` 的概念验证。依赖相关报告需证明已随发行物交付的依赖受影响，且问题可通过 Pi 触达。暴露密钥的报告需证明该凭证属于 Earendil，或能访问 Earendil 运营的基础设施或服务。
