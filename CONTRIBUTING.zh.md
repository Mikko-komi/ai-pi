# 为 pi 做贡献

> 本文为 [CONTRIBUTING.md](CONTRIBUTING.md) 的中文译本。

本指南是为了节省双方时间。

## 理念

先说清楚：**pi 的核心必须保持精简**。

若功能不属于核心，就应该做成扩展。会把核心做臃肿的 PR 多半会被拒绝。

Pi 的核心存在的意义是保持精简且可扩展，以便被扩展影响和操控。即便是给扩展用的 hook 点，也要慎重考虑和讨论，避免不可维护的膨胀和复杂交互。

## 唯一规则

**你必须理解自己的代码。** 若无法解释改动做了什么、以及它如何与系统其余部分交互，PR 会被关闭。

用 AI 写代码可以。提交自己看不懂的 AI 生成垃圾代码不行。

若使用 agent，请从 `pi` 仓库根目录运行，以便自动加载 `AGENTS.md`。你的 agent 必须遵守该文件中的规则和指南。

## 贡献门槛

新贡献者的所有 issue 和 PR 默认会被自动关闭。

周五至周日提交的 issue 不保证会被审阅。若紧急，请到 Discord 询问：https://discord.com/invite/3cU7Bz4UPx

维护者每天审阅被自动关闭的 issue，并重开值得跟进的。达不到下方质量标准的 issue 不会被重开，也不会得到回复。

审批通过维护者在 issue 上的回复完成：

- `lgtmi`：你之后的 issue 不再被自动关闭
- `lgtm`：你之后的 issue 和 PR 都不再被自动关闭

该命令必须出现在回复开头（可选地跟在一个或多个 `@username` 之后）或结尾。`lgtmi` 不授予提交 PR 的权利。只有 `lgtm` 才允许提交 PR。

## Issue 质量标准

开 issue 必须使用两种 GitHub issue 模板之一。

开 issue 时要短、具体、值得一读。

- 保持简洁。一屏放不下就太长。
- 用你自己的口吻写（不要用 LLM 生成正文；若必须用，请再跟一条明确标注为 AI 的评论）。
- 清楚陈述 bug 或请求。
- 说明为什么重要。
- 若打算自己实现，请写明。

若问题真实且写得好，维护者可能重开，或在上述命令位置回复 `lgtmi` / `lgtm`。

## 封禁

若两次无视本文档，或用 agent 生成的 issue 刷 tracker，你的 GitHub 账号会被永久封禁。

若通过自动化大量发送 issue，账号会被永久封禁。没有反悔。

## 提交 PR 之前

除非维护者已在上述命令位置给出 `lgtm`，否则不要开 PR。

提交 PR 前：

```bash
npm run check
./test.sh
```

两者都必须通过。

不要编辑 `CHANGELOG.md`。changelog 条目由维护者添加。

若要向 `packages/ai` 添加新提供方，见 `AGENTS.md` 中的必测项。

## 有问题？

到 [Discord](https://discord.com/invite/nKXTsAcmbT) 问。

## FAQ

### 为什么新 issue 和 PR 会被自动关闭？

pi 收到的 issue 多于维护者能实时负责审阅的数量。许多报告达不到本指南的质量标准，或不遵守 CONTRIBUTING.md。有些是被 agent 不经思考甩到仓库上的，提交者自己都没审过、没改过。自动关闭形成缓冲，让维护者按自己的节奏审阅 tracker，并重开达到质量标准的 issue。

### 为什么周末的 issue 优先级更低？

我们在工作时间分流 tracker。周末会堆积更多 issue。周五至周日提交的内容可能在周一审阅队列里被漏掉或降优先级。若问题紧急，到 Discord 说明：短摘要、复现步骤和相关日志。

### 为什么有些 issue 没有回复？

回复本身也是维护工作。低信号、含糊、重复、或不遵守本指南的 issue 可能直接关闭、不加讨论。这样才能把时间留给可复现的 bug、经过思考的请求，以及已经把报告写到可执行程度的贡献者。

### 为什么不让 AI 做全部分流？

AI 可以帮助归并重复、摘要报告、发现缺信息。它不被信任做最终维护者决策。措辞漂亮的 AI 生成 issue 仍可能错误、误导，或调查成本很高。最终门槛仍是人工审阅。

### 这对贡献者不友好吗？

不是。这是防止倦怠和 tracker 垃圾信息的护栏。短、具体、可复现的 issue 受欢迎。经过思考的贡献受欢迎。自动化垃圾、理所当然的态度、以及大量低投入报告不受欢迎。

## 在哪里了解规划？

Earendil 用 RFC 讨论较大变更。并非全部公开，但有不少是公开的。见 [rfc.earendil.com](https://rfc.earendil.com/keyword/pi/)。
