/**
 * Stable provider id for the built-in Radius OAuth provider.
 *
 * Radius OAuth provider 的稳定 id。models.json 的 `oauth: "radius"` 必须对上这个值。
 */

/**
 * Canonical provider id for Radius.
 *
 * Radius 的规范 provider id。不要在别处再写字面量 `"radius"`。
 */
export const RADIUS_PROVIDER_ID = "radius";
