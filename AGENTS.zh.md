# 开发规则

> 本文为 [AGENTS.md](AGENTS.md) 的中文译本。Agent 仍应遵守英文原文 `AGENTS.md`。

## 对话风格

- 回答保持短而精
- commit、issue、PR 评论或代码中不要用 emoji
- 不要客套或欢呼式废话（例如写 “Thanks @user”，不要写 “Thanks so much @user!”）
- 只用技术性文字，直接说
- 用简洁、清楚、简单的语言。无法避免的行话先定义再使用。
- 解释非平凡设计与问题时按：问题、具体例子或短 trace，然后是方案。说明方案为什么必要，并把它与可选复杂度区分开。
- 优先写具体行为和小组例子，而不是抽象摘要、密集术语或未解释的变更列表。
- 用户提问时先回答问题，再改代码或跑实现命令。
- 回应用户反馈或分析时，先明确同意还是不同意，再说改了什么。

## 代码质量

- 做大范围改动前、编辑尚未完整读过的文件前、以及被要求调查或审计时，先完整读文件。不要靠搜索片段做大改。
- 除非绝对必要，不要用 `any`。
- 只有一个调用点的单行 helper 就地内联。
- 查 `node_modules` 里的外部 API 类型；不要猜。
- **禁止内联 import**（`await import()`、`import("pkg").Type`、动态类型 import）。只用顶层 import。
- 不要为了修过时依赖导致的类型错误而删除或降级代码；应升级该依赖。
- 受根配置检查的代码（`packages/*/src`、`packages/*/test`、`packages/coding-agent/examples`）只用可擦除的 TypeScript 语法（Node strip-only 模式）：不要用参数属性、`enum`、`namespace`/`module`、`import =`、`export =`，或其他需要 JS 发射的构造。用显式字段并在构造函数里赋值。
- 删除看起来是有意存在的功能或代码前，必须先问。
- 除非用户要求，不要保留向后兼容。
- 不要硬编码按键检查（例如 `matchesKey(keyData, "ctrl+x")`）。把默认值加到 `DEFAULT_EDITOR_KEYBINDINGS` 或 `DEFAULT_APP_KEYBINDINGS`，保持可配置。
- 不要直接改 `packages/ai/src/models.generated.ts`；应改 `packages/ai/scripts/generate-models.ts` 再重新生成。把生成后的 `models.generated.ts` 差异一并提交始终可以，即使再生包含无关的上游模型元数据变更。

## 命令

- 改代码后（不是文档）：跑 `npm run check`（完整输出，不要 tail）。提交前修掉所有 error、warning、info。它不跑测试。
- 除非用户要求，不要跑 `npm run build` 或 `npm test`。
- 不要直接跑完整 vitest 套件：其中包含 e2e，会在存在 endpoint/auth 环境变量时激活。所有非 e2e 测试从仓库根目录跑 `./test.sh`。否则从包根目录跑指定测试：
  - Vitest：`node "$(git rev-parse --show-toplevel)/node_modules/vitest/dist/cli.js" --run test/specific.test.ts`
  - `packages/tui`（`node:test`）：`node --test test/specific.test.ts`
- 若创建或修改了测试文件，跑它并迭代测试或实现直到通过。
- 对 `packages/coding-agent/test/suite/`，使用 `test/suite/harness.ts` + faux provider。不要用真实提供方 API、密钥或付费 token。
- 为修复 GitHub issue 写回归测试时，在测试旁加该 issue 编号的注释。
- 临时脚本写到临时文件（例如 `/tmp`），跑、按需改、用完删除。不要把多行脚本嵌进 `bash` 命令。
- 除非用户要求，不要 commit。

## 依赖与安装安全

- 把 npm 依赖和 lockfile 变更当作经过审阅的代码。直接外部依赖钉死到精确版本。
- 更新 `undici` 时，必须阅读目标版本的 changelog / release notes，评估是否影响功能后再更新。
- 本地灌装 / 更新用 `npm install --ignore-scripts`；干净 / CI 风格用 `npm ci --ignore-scripts`。除非用户要求，不要跑生命周期脚本。
- 若依赖元数据变了，用 `npm install --package-lock-only --ignore-scripts` 刷新 `package-lock.json`。
- 若需要重新生成 `packages/coding-agent/npm-shrinkwrap.json`，跑 `node scripts/generate-coding-agent-shrinkwrap.mjs`（用 `--check` 或 `npm run check` 验证）。带生命周期脚本的新依赖需要审阅，并在该脚本中显式加入白名单；不要悄悄添加。
- 除非 `PI_ALLOW_LOCKFILE_CHANGE=1`，pre-commit 会拦截 lockfile 提交。除非用户希望提交 lockfile 变更，不要绕过。

## Git

同一工作目录可能同时跑多个 pi 会话，各自改不同文件。会碰到你改动之外的未暂存、已暂存或未跟踪文件的 git 操作，会踩掉其他会话的工作。遵守以下规则：

提交时：

- 只提交你在**本会话**改过的文件。
- 用显式路径暂存（`git add <path1> <path2>`）；不要 `git add -A` / `git add .`。
- 提交前跑 `git status`，确认只暂存了你的文件。
- `packages/ai/src/models.generated.ts` 可以始终与你的文件一起纳入。
- 信息格式：`{feat,fix,docs}[(ai,tui,agent,coding-agent)]: <commit message>`（可选多行）。信息要有用且简洁。

永远不要跑（会毁掉其他 agent 的工作或绕过检查）：

- `git reset --hard`、`git checkout .`、`git clean -fd`、`git stash`、`git add -A`、`git add .`、`git commit --no-verify`。

若 rebase 发生冲突：

- 只解决你改过的文件中的冲突。
- 若冲突在你未改过的文件里，中止并问用户。
- 永远不要 force push。

## Issue 与 PR

贡献门槛见 `CONTRIBUTING.md`（自动关闭工作流、`lgtm`/`lgtmi`、质量标准）。

审阅 PR 时：

- 除非用户明确要求，不要跑 `gh pr checkout`、`git switch`，或把工作树切到 PR 分支。
- 用 `gh pr view`、`gh pr diff`、`gh api`，以及针对已 fetch 的 ref 的本地 `git show`/`git diff` 查看 PR 元数据、commit 和补丁，不要换分支。
- 若需要 PR 文件内容，抓到临时文件，或用 `git show <ref>:<path>`，不要切换分支。

创建 issue 时：

- 为受影响的包加上 `pkg:*` 标签（`pkg:agent`、`pkg:ai`、`pkg:coding-agent`、`pkg:tui`）；适用的都加上。

发 issue / PR 评论时：

- 把评论写到临时文件，用 `gh issue/pr comment --body-file` 发布（不要用 `--body` 传多行 markdown）。
- 评论保持简洁、技术性，贴近用户语气。
- 每条 AI 发布的评论都以发起提示词指定的 AI 免责声明结尾（例如 `This comment is AI-generated by `/wr``）。

通过 commit 关闭 issue 时：

- 在信息中包含 `fixes #<number>` 或 `closes #<number>`，以便合并时自动关闭。多个 issue 时每个都重复关键字（`closes #1, closes #2`）；共用一个关键字（`closes #1, #2`）只会关闭第一个。

## 用 tmux 测试 pi 交互模式

测试 pi 交互模式时，加载并遵循 [.pi/skills/interactive-testing.md](.pi/skills/interactive-testing.md)。

## Changelog

位置：`packages/*/CHANGELOG.md`（每包一份）。

`## [Unreleased]` 下的小节：`### Breaking Changes`（需要迁移的 API 变更）、`### Added`、`### Changed`、`### Fixed`、`### Removed`。

规则：

- 新条目全部放在 `## [Unreleased]` 下。先读完整节，再追加到已有小节；不要重复创建小节。
- 已发布版本小节（例如 `## [0.12.2]`）不可改。
- 在非 `main` 且非 pull request 的分支上工作时，不要写 changelog 条目。

归属：

- 内部（来自 issue）：`Fixed foo bar ([#123](https://github.com/earendil-works/pi/issues/123))`
- 外部贡献：`Added feature X ([#456](https://github.com/earendil-works/pi/pull/456) by [@username](https://github.com/username))`

## 发布

准备发布、发布、验证或恢复时，加载并遵循 [.pi/skills/release.md](.pi/skills/release.md)。

## 用户覆盖

若用户指示与本文档中的规则冲突，先征求明确确认，再执行其指示。
